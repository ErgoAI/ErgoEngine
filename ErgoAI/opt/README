To enable compiler optimization, create empty file named
opt.switch.on
To disable the optimization, rename it to opt.switch.off

The disadvantages of optimization is that it does not speed up
thingsmuch, but all the compile-time warnings from XSB will be lost.
